// ``(백틱)을 사용하면 ES6에서 Template Literal을 사용할 수 있다.

let multiLine = `This is a
string that spans
multiple lines.`;

console.log(multiLine);

// ES6 Variable Interpolation을 사용하면 변수를 문자열에 삽입할 수 있다.

let name = 'John';
let age = 25;

let sentence = `My name is ${name} and I am ${age} years old.`;

